## Prerequisites

Docker engine installed and running

Docker Compose installed

Deployment Steps

Create a new directory for the deployment and copy the compose file into it.

Open a terminal or command prompt and navigate to the deployment directory.

Run the following command to start the services: